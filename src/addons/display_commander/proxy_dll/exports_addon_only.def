EXPORTS
	; Reference only: minimal export table (AddonInit + GetDisplayCommanderState). Not linked by default.
	; Default builds use full proxy_dll/exports.def and compile proxy_dll/*.cpp (rename-d3d11/dxgi/etc. supported).
	; ReShade addon metadata (NAME, AUTHOR, …) and GetDisplayCommanderVersion / LoadedNs are exported via
	; __declspec(dllexport) in addon.cpp.
	AddonInit
	GetDisplayCommanderState
